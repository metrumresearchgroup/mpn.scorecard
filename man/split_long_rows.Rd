% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format-report.R
\name{split_long_rows}
\alias{split_long_rows}
\title{Split Long Rows in a Data Frame by Newline Count}
\usage{
split_long_rows(exported_func_df, n = 45)
}
\arguments{
\item{exported_func_df}{A data frame containing columns \code{code_file},
\code{documentation}, and \code{test_files}, where the contents are character strings
that may span multiple lines.}

\item{n}{An integer specifying the maximum number of newline characters
allowed in each row's content. Default is 45.}
}
\value{
A data frame with rows split based on newline character count.
Each original row that exceeds the specified number of lines will be split
into multiple rows with continuation labels.
}
\description{
This function processes a data frame to split rows where the content in
specific columns exceeds a specified number of newline characters.
Rows with content that spans more than \code{n} lines will be split into
multiple rows, with the excess content carried over into continuation
rows. The new rows will be labeled with "(cont.)" to indicate continuation.
}
\note{
This function assumes the \code{entry_name}, a column determined early on in
\code{format_traceability_matrix}, is the first column in the dataframe.
}
\examples{
\dontrun{

split_df <- split_long_rows(exported_func_df)
}
}
\keyword{internal}
